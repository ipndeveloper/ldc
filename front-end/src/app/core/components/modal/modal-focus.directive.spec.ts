import { ModalFocusDirective } from './modal-focus.directive';

describe('ModalFocusDirective', () => {
  it('should create an instance', () => {
    const directive = new ModalFocusDirective({} as any);
    expect(directive).toBeTruthy();
  });
});
