import { DecimalSeparatorPipe } from './decimal-separator.pipe';

describe('DecimalSeparatorPipePipe', () => {
  it('create an instance', () => {
    const pipe = new DecimalSeparatorPipe();
    expect(pipe).toBeTruthy();
  });
});
