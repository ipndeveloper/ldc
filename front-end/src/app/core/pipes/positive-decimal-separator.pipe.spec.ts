import { PositiveDecimalSeparatorPipe } from './positive-decimal-separator.pipe';

describe('PositiveDecimalSeparatorPipe', () => {
  it('create an instance', () => {
    const pipe = new PositiveDecimalSeparatorPipe();
    expect(pipe).toBeTruthy();
  });
});
